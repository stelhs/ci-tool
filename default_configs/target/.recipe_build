#!/bin/bash -e

# This script builds target and returns paths to build products in .build_result file
# $SESSION_DIR - path to the session directory


# building procedure...

# echo "result_name1: /path/to/binary1" >> $SESSION_DIR/.build_result
# echo "result_name2: /path/to/binary2" >> $SESSION_DIR/.build_result

